apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: create-repo-3
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/platform-nexus-rn/platform-nexus-deployments.git
    targetRevision: main
    path: apps/create-repo-3/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: create-repo-3
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
