apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: create-repo-2
  namespace: kube-prometheus-stack
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: create-repo-2
  namespaceSelector:
    matchNames:
      - create-repo-2
  endpoints:
    - port: metrics
      interval: 5s
