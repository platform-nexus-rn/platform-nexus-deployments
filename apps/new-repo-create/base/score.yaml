apiVersion: score.dev/v1b1
metadata:
  name: new-repo-create
service:
  ports:
    metrics:
      port: 8080
      targetPort: 8080
      protocol: TCP
containers:
  app:
    image: ghcr.io/platform-nexus-rn/new-repo-create
    variables:
      FLASK_ENV: development
