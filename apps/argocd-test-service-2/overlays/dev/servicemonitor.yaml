apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: argocd-test-service-2
  namespace: kube-prometheus-stack
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-test-service-2
  namespaceSelector:
    matchNames:
      - argocd-test-service-2
  endpoints:
    - port: metrics
      interval: 5s
