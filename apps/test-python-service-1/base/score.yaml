apiVersion: score.dev/v1b1
metadata:
  name: test-python-service-1
service:
  ports:
    metrics:
      port: 8080
      targetPort: 8080
      protocol: TCP
containers:
  app:
    image: ghcr.io/platform-nexus-rn/test-python-service-1
    variables:
      FLASK_ENV: development
