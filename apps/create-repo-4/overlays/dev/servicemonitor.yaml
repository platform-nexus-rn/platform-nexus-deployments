apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: create-repo-4
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: create-repo-4
  namespaceSelector:
    matchNames:
      - create-repo-4
  endpoints:
    - port: metrics
      interval: 5s
