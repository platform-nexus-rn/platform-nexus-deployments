apiVersion: score.dev/v1b1
metadata:
  name: my-test-python-service-6
service:
  ports:
    metrics:
      port: 8080
      targetPort: 8080
      protocol: TCP
containers:
  app:
    image: ghcr.io/platform-nexus-rn/my-test-python-service-6
    variables:
      FLASK_ENV: development
